完全用 Python 即可。 你的核心模块全都是 Python 生态的强项：

- 数据采集：tushare / akshare（Python 库）
- Agent 编排：LangGraph（Python）
- ML 预测：scikit-learn / XGBoost / PyTorch（Python）
- 情感分析：transformers / LangChain（Python）
- 后端 API：FastAPI（Python）
- 定时调度：APScheduler（Python）

前端可以后期再加（或者用 Streamlit/Gradio 快速出一个可视化界面），当前阶段不需要 Vue。

------

## 建议的工程重构方向

你现在的问题是规划太散，代码太少，目录结构过度设计。建议按以下结构重新整理，遵循"先跑通再优化"的原则：

```
-Attribution-Analysis/
├── README.md                    # 项目简介 + 快速启动
├── pyproject.toml               # 统一依赖管理 (替代 requirements.txt)
├── .env.example                 # 环境变量模板
│
├── src/                         # 所有源码在这里
│   ├── __init__.py
│   │
│   ├── config.py                # 全局配置
│   │
│   ├── data/                    # 数据采集层
│   │   ├── __init__.py
│   │   ├── tushare_client.py    # tushare 数据获取（主）
│   │   ├── akshare_client.py    # akshare 数据获取（辅）
│   │   └── news_crawler.py      # 新闻/公告抓取
│   │
│   ├── analysis/                # 分析层
│   │   ├── __init__.py
│   │   ├── anomaly_detector.py  # 异常检测 (统计 + ML)
│   │   ├── sentiment.py         # 新闻情感分析
│   │   ├── ml_predictor.py      # ML 预测模型
│   │   └── attribution.py       # 归因分析 (LLM)
│   │
│   ├── agent/                   # LangGraph Agent
│   │   ├── __init__.py
│   │   ├── graph.py             # 主 Agent 图定义
│   │   ├── state.py             # Agent 状态定义
│   │   ├── nodes.py             # 各节点实现
│   │   └── tools.py             # Agent 可调用的工具
│   │
│   ├── db/                      # 数据库层
│   │   ├── __init__.py
│   │   ├── models.py            # ORM 模型
│   │   └── repository.py        # 数据访问封装
│   │
│   └── api/                     # FastAPI 接口 (可选，后期加)
│       ├── __init__.py
│       └── routes.py
│
├── scripts/                     # 独立脚本
│   ├── run_agent.py             # 启动 Agent
│   ├── backfill_data.py         # 历史数据回填
│   └── train_model.py           # 训练 ML 模型
│
├── tests/                       # 测试
│   └── ...
│
└── docs/                        # 精简文档
    ├── architecture.md
    └── development.md
```



### 核心原则

1. 砍掉前端规划——当前用 Streamlit 或纯 CLI 输出即可，等核心链路跑通再说
2. 砍掉 Docker——本地开发阶段直接跑 Python，部署时再容器化
3. 用 `pyproject.toml` 替代 `requirements.txt`——统一管理依赖和项目元数据
4. 先跑通最小闭环——一个脚本 `run_agent.py` 就能触发整个链路

------

## 建议的开发顺序（最小可行路径）

Week 1:  tushare_client.py → 能稳定拉取数据入库

Week 2:  anomaly_detector.py → 基于统计规则检出异常

Week 3:  agent/graph.py → LangGraph 串联 "采集→检测→归因"

Week 4:  sentiment.py + news_crawler.py → 新闻情感接入

Week 5:  ml_predictor.py → ML 模型训练和推理

Week 6:  整合优化 + 定时调度 + 通知

每一步都是可独立验证的——不需要所有模块都就绪才能看到效果。